# Censys-Go — CLI Documentation

<div id="header" align="center">
    <img src="https://media3.giphy.com/media/v1.Y2lkPTc5MGI3NjExcnlxcXUxaHhsa2J0N3ZranM2a3RxaXUyaWRpZW96bHoxY2poaXJ3bCZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9Zw/q15lIdQWBYs7K/giphy.gif" width="200"/>
</div>

_Generated by `scripts/generate_docs.py`. Edit the Go doc comments, not this file._

## Table of contents

1. [Package `main`](#package-main) — `.`
2. [Package `cli`](#package-cli) — `internal/cli`
3. [Package `censysx`](#package-censysx) — `internal/censysx`
4. [Package `config`](#package-config) — `internal/config`
5. [Package `render`](#package-render) — `internal/render`
6. [Package `hunt`](#package-hunt) — `internal/hunt`
7. [Package `ui`](#package-ui) — `internal/ui`

---

## Package `main`

Command censys_go is an interactive and scriptable client for the Censys API,
built on the official SDK at https://github.com/censys/censys-sdk-go (MIT).

Directory: `.`

Files: `main.go`

| Declaration | Description |
|---|---|
| `version` | version is stamped at build time via -ldflags "-X main.version=...". |
| `const block` | Exit codes: 2 for a malformed command line, 130 for an interrupt, 1 otherwise. |

### `version`

version is stamped at build time via -ldflags "-X main.version=...".

### `const block`

Exit codes: 2 for a malformed command line, 130 for an interrupt, 1 otherwise.

---

## Package `cli`

Package cli implements the command-line surface: global flags, subcommand
dispatch, and the wiring between them and the internal packages.

Results go to Env.Out (stdout) and everything else to Env.Msg (stderr), so
`censys search -q ... > hits.ndjson` yields a file containing only results.

Directory: `internal/cli`

Files: `args.go`, `cli.go`, `commands.go`

| Declaration | Description |
|---|---|
| `splitCommand()` | splitCommand finds the subcommand name in args and returns it along with the |
| `permute()` | permute reorders args so that every flag precedes every positional argument. |
| `isFlag()` | isFlag reports whether a token looks like a flag rather than an argument. |
| `consumesNext()` | consumesNext reports whether a flag token takes the following token as its |
| `ErrUsage` | ErrUsage signals that the command line itself was wrong, so the caller can |
| `Interrupted()` | Interrupted reports whether err is the consequence of ctx being cancelled |
| `Env` | Env carries the process environment a command runs against. |
| `globals` | globals holds the flags every subcommand shares. |
| `command` | command is one subcommand. |
| `Run()` | Run parses args and dispatches to a subcommand. With no arguments it starts |
| `examples` | CenQL requires the dataset prefix on every field, so each example carries it. |
| `session` | session bundles everything a command needs at run time. |
| `newSession()` | newSession resolves credentials, builds the client, and opens the output |
| `commands()` | commands returns the subcommand table. |
| `runSearch()` | runSearch streams every page of a query through the session's renderer. |
| `runHosts()` | runHosts fetches targets through the batch endpoint, which costs one request |
| `runHostsOneByOne()` | runHostsOneByOne fetches each target separately. It costs the same in credits |
| `runCertHosts()` | runCertHosts streams the observation ranges for a certificate. |
| `valueOrZero()` | valueOrZero dereferences an optional time, yielding the zero value for nil. |
| `collectTargets()` | collectTargets gathers targets from arguments, a file, or stdin, in that order. |
| `parseAtTime()` | parseAtTime accepts an RFC3339 timestamp or a bare date. |

### `splitCommand()`

splitCommand finds the subcommand name in args and returns it along with the
remaining arguments in their original order.

It walks the tokens the way flag.Parse would, so a flag's value is never
mistaken for the command: in `censys --format table host 1.2.3.4`, "table" is
consumed as the value of --format and "host" is the command. known describes
the global flags, which are the only ones that may appear before the command.

### `permute()`

permute reorders args so that every flag precedes every positional argument.
The standard flag package stops parsing at the first positional, which would
make `censys host 1.2.3.4 --format csv` silently treat "--format" as a target.

Relative order is preserved within each group, and everything after a literal
"--" is left untouched as positional.

### `isFlag()`

isFlag reports whether a token looks like a flag rather than an argument.

### `consumesNext()`

consumesNext reports whether a flag token takes the following token as its
value. Boolean flags never do, and neither does a token that already carries
its value as "-name=value". An unrecognised flag is assumed not to, leaving
flag.Parse to report it.

### `ErrUsage`

ErrUsage signals that the command line itself was wrong, so the caller can
exit 2 rather than 1.

### `Interrupted()`

Interrupted reports whether err is the consequence of ctx being cancelled
rather than a genuine failure.

Testing the error is not enough. signal.NotifyContext attaches a cause, and
net/http reports that cause instead of context.Canceled, so
errors.Is(err, context.Canceled) is false for anything that failed in
transit. The context itself is the reliable signal.

### `Env`

Env carries the process environment a command runs against.

### `globals`

globals holds the flags every subcommand shares.

### `command`

command is one subcommand.

### `Run()`

Run parses args and dispatches to a subcommand. With no arguments it starts
the interactive menu.

### `examples`

CenQL requires the dataset prefix on every field, so each example carries it.

### `session`

session bundles everything a command needs at run time.

### `newSession()`

newSession resolves credentials, builds the client, and opens the output
sink. The returned cleanup closes the sink if it is a file.

### `commands()`

commands returns the subcommand table.

### `runSearch()`

runSearch streams every page of a query through the session's renderer.

### `runHosts()`

runHosts fetches targets through the batch endpoint, which costs one request
per hundred hosts instead of one per host.

### `runHostsOneByOne()`

runHostsOneByOne fetches each target separately. It costs the same in credits
but many more round trips; its value is that a failure names the host that
caused it, which the batch endpoint cannot do.

### `runCertHosts()`

runCertHosts streams the observation ranges for a certificate.

### `valueOrZero()`

valueOrZero dereferences an optional time, yielding the zero value for nil.

### `collectTargets()`

collectTargets gathers targets from arguments, a file, or stdin, in that order.

### `parseAtTime()`

parseAtTime accepts an RFC3339 timestamp or a bare date.

---

## Package `censysx`

Package censysx wraps the Censys SDK with the retry policy, pagination and
typed accessors this CLI needs, so command code never touches raw envelopes.

Directory: `internal/censysx`

Files: `asset.go`, `batch.go`, `client.go`, `credits.go`, `errors.go`, `model.go`, `pivot.go`, `search.go`

| Declaration | Description |
|---|---|
| `Client.Host()` | Host fetches one host. at selects a historical snapshot; nil means current. |
| `Client.Certificate()` | Certificate fetches one certificate by its SHA-256 fingerprint. |
| `NormalizeFingerprint()` | NormalizeFingerprint lower-cases and validates a SHA-256 certificate |
| `MaxBatchSize` | MaxBatchSize is the largest host batch sent in one request. |
| `Client.HostsBatch()` | HostsBatch fetches up to MaxBatchSize hosts in a single request. Prefer |
| `Client.HostsEach()` | HostsEach fetches every host in ids, chunked into batches, calling fn for each |
| `const block` | Defaults applied when Options leaves a field at its zero value. |
| `retryBudgetRatio` | retryBudgetRatio is the share of Timeout the retry loop may consume. Leaving |
| `Options` | Options configures a Client. |
| `Client` | Client is a thin, typed facade over the Censys SDK. |
| `New()` | New builds a Client. Retries are delegated to the SDK, which already applies |
| `Client.OrgID()` | OrgID returns the organization the client acts on behalf of. |
| `Client.SDK()` | SDK exposes the underlying SDK for calls this package does not wrap yet. |
| `backoff()` | backoff builds the SDK retry policy. retry.BackoffStrategy takes milliseconds. |
| `Client.Credits()` | Credits returns the organization's current credit balance. |
| `Client.CreditUsage()` | CreditUsage returns the daily credit usage report covering the last `days` days. |
| `NextExpiry()` | NextExpiry returns the soonest credit expiry, or nil if none is scheduled. |
| `StatusOf()` | StatusOf returns the HTTP status carried by err, or 0 if it carries none. |
| `IsAuth()` | IsAuth reports whether err is a credential problem rather than a transient one. |
| `IsNotFound()` | IsNotFound reports whether the requested asset is simply absent from Censys. |
| `Explain()` | Explain renders err for a human, unwrapping the SDK's JSON-blob error strings |
| `HostRecord` | HostRecord is a flattened view of a host, holding the fields worth putting in |
| `ServiceRecord` | ServiceRecord is one exposed service, reduced to the fields that identify it |
| `HostRecord.Ports()` | Ports returns the host's open ports in ascending order. |
| `HostRecord.CertHashes()` | CertHashes returns the distinct certificate SHA-256 digests served by the host. |
| `HostRecord.JARMHashes()` | JARMHashes returns the distinct JARM fingerprints observed on the host. |
| `NewHostRecord()` | NewHostRecord flattens a Host into a HostRecord using the SDK's typed getters. |
| `softwareLabel()` | softwareLabel renders an Attribute as "vendor product version", skipping the |
| `HostRecordsFromHits()` | HostRecordsFromHits flattens search hits, keeping only host results. Certificate |
| `IPsFromHits()` | IPsFromHits returns the distinct IPs in a set of search hits, in result order. |
| `distinct()` | distinct collects the non-empty strings produced by push, dropping duplicates |
| `maxObservationPageSize` | maxObservationPageSize is the cap the observations endpoint documents. |
| `ObservationCreditsPerPage` | ObservationCreditsPerPage is what the observations endpoint costs. It is an |
| `CertObservationParams` | CertObservationParams narrows a certificate-observation lookup. |
| `Client.CertObservations()` | CertObservations reports every host seen serving a given certificate, as |
| `Client.Timeline()` | Timeline returns a host's service and certificate change history between two |
| `DefaultSearchFields` | DefaultSearchFields is the field set requested from Search. Keeping it tight |
| `SearchParams` | SearchParams describes one search request. |
| `SearchPage` | SearchPage is one page of results. |
| `ErrEmptyQuery` | ErrEmptyQuery is returned when a search is attempted without a query string. |
| `Client.Search()` | Search fetches a single page of results, starting from pageToken (empty for |
| `Client.SearchEach()` | SearchEach walks result pages, calling fn for each one. It stops after |
| `Client.Aggregate()` | Aggregate buckets a query by field, for quick distribution analysis. |

### `Client.Host()`

Host fetches one host. at selects a historical snapshot; nil means current.

### `Client.Certificate()`

Certificate fetches one certificate by its SHA-256 fingerprint.

### `NormalizeFingerprint()`

NormalizeFingerprint lower-cases and validates a SHA-256 certificate
fingerprint, rejecting anything that is not 64 hex characters.

### `MaxBatchSize`

MaxBatchSize is the largest host batch sent in one request.

### `Client.HostsBatch()`

HostsBatch fetches up to MaxBatchSize hosts in a single request. Prefer
HostsEach, which chunks a longer list for you.

### `Client.HostsEach()`

HostsEach fetches every host in ids, chunked into batches, calling fn for each
host returned.

The batch endpoint is one request per chunk rather than one per host, which
is the difference between a handful of round trips and several hundred when
sweeping a result set. Hosts Censys has no record of are simply absent from
the response; missing reports how many were dropped that way.

### `const block`

Defaults applied when Options leaves a field at its zero value.

### `retryBudgetRatio`

retryBudgetRatio is the share of Timeout the retry loop may consume. Leaving
headroom means a call that exhausts its retries reports the last HTTP error
rather than a bare "context deadline exceeded".

### `Options`

Options configures a Client.

### `Client`

Client is a thin, typed facade over the Censys SDK.

### `New()`

New builds a Client. Retries are delegated to the SDK, which already applies
exponential backoff with jitter, honours Retry-After, retries only
429/500/502/503/504, and aborts on context cancellation.

### `Client.OrgID()`

OrgID returns the organization the client acts on behalf of.

### `Client.SDK()`

SDK exposes the underlying SDK for calls this package does not wrap yet.

### `backoff()`

backoff builds the SDK retry policy. retry.BackoffStrategy takes milliseconds.

### `Client.Credits()`

Credits returns the organization's current credit balance.

### `Client.CreditUsage()`

CreditUsage returns the daily credit usage report covering the last `days` days.

### `NextExpiry()`

NextExpiry returns the soonest credit expiry, or nil if none is scheduled.

### `StatusOf()`

StatusOf returns the HTTP status carried by err, or 0 if it carries none.

### `IsAuth()`

IsAuth reports whether err is a credential problem rather than a transient one.

### `IsNotFound()`

IsNotFound reports whether the requested asset is simply absent from Censys.

### `Explain()`

Explain renders err for a human, unwrapping the SDK's JSON-blob error strings
into something that fits on one line.

### `HostRecord`

HostRecord is a flattened view of a host, holding the fields worth putting in
a table, a CSV row, or a pivot query. The full asset is kept in Raw for
callers that need everything.

### `ServiceRecord`

ServiceRecord is one exposed service, reduced to the fields that identify it
and the fingerprints that can be pivoted on.

### `HostRecord.Ports()`

Ports returns the host's open ports in ascending order.

### `HostRecord.CertHashes()`

CertHashes returns the distinct certificate SHA-256 digests served by the host.

### `HostRecord.JARMHashes()`

JARMHashes returns the distinct JARM fingerprints observed on the host.

### `NewHostRecord()`

NewHostRecord flattens a Host into a HostRecord using the SDK's typed getters.

### `softwareLabel()`

softwareLabel renders an Attribute as "vendor product version", skipping the
parts the scan did not identify.

### `HostRecordsFromHits()`

HostRecordsFromHits flattens search hits, keeping only host results. Certificate
and web-property hits are skipped; a query can return a mix of all three.

### `IPsFromHits()`

IPsFromHits returns the distinct IPs in a set of search hits, in result order.

### `distinct()`

distinct collects the non-empty strings produced by push, dropping duplicates
and preserving first-seen order.

### `maxObservationPageSize`

maxObservationPageSize is the cap the observations endpoint documents.

### `ObservationCreditsPerPage`

ObservationCreditsPerPage is what the observations endpoint costs. It is an
order of magnitude more than an ordinary lookup, so callers surface it before
walking a long result set.

### `CertObservationParams`

CertObservationParams narrows a certificate-observation lookup.

### `Client.CertObservations()`

CertObservations reports every host seen serving a given certificate, as
time-bounded ranges.

This is the pivot that turns a single sample into a campaign: take the
certificate off one panel and the endpoint returns the rest of the fleet,
including hosts that have since gone dark and so no longer appear in a live
search.
It reports the total the API claims and how many pages were actually
fetched, so the caller can account for what the walk cost.

### `Client.Timeline()`

Timeline returns a host's service and certificate change history between two
instants.

The API names its window fields from the operator's point of view rather than
chronologically: StartTime is the end of the window nearest to now and
EndTime the one furthest away. This wrapper takes the range the ordinary way
round and swaps it.

### `DefaultSearchFields`

DefaultSearchFields is the field set requested from Search. Keeping it tight
keeps responses small.

Every name is a documented CenQL path and maps onto a field the response
model carries. Note the certificate digest: the docs name
host.services.cert.fingerprint_sha256 as the field to pivot on, and the
flattener reads it from the same place.

### `SearchParams`

SearchParams describes one search request.

### `SearchPage`

SearchPage is one page of results.

### `ErrEmptyQuery`

ErrEmptyQuery is returned when a search is attempted without a query string.

### `Client.Search()`

Search fetches a single page of results, starting from pageToken (empty for
the first page).

### `Client.SearchEach()`

SearchEach walks result pages, calling fn for each one. It stops after
maxPages (0 means every page), when a page reports no continuation token, or
as soon as fn returns an error, which is passed through to the caller.

Each request gets the client's per-request timeout; ctx bounds the walk as a
whole, so cancelling it stops the iteration between pages.

### `Client.Aggregate()`

Aggregate buckets a query by field, for quick distribution analysis.

---

## Package `config`

Package config resolves and persists Censys API credentials.

Directory: `internal/config`

Files: `config.go`

| Declaration | Description |
|---|---|
| `Dir` | Dir is the credential directory, relative to the user's home directory. |
| `FileName` | FileName is the credential file inside Dir. |
| `const block` | Environment variables consulted by FromEnv. |
| `Credentials` | Credentials identify an organization and the token used to act on its behalf. |
| `Source` | Source records where a set of credentials came from, so the CLI can report it |
| `ErrNotConfigured` | ErrNotConfigured is returned by Resolve when no source supplied credentials. |
| `Credentials.Validate()` | Validate reports whether both fields are populated. |
| `Credentials.complete()` | complete reports whether both fields are set, without allocating an error. |
| `Credentials.Redacted()` | Redacted returns a copy safe to print or serialize in diagnostics. |
| `Path()` | Path returns the absolute path of the credential file. |
| `Load()` | Load reads credentials from disk. A missing file is reported as os.ErrNotExist, |
| `Save()` | Save writes credentials to disk with owner-only permissions. |
| `FromEnv()` | FromEnv reads credentials from the environment. Unset variables yield empty |
| `Resolve()` | Resolve picks credentials from the first source that supplies both fields: |
| `homeDir()` | homeDir resolves the user's home directory, falling back to the conventional |

### `Dir`

Dir is the credential directory, relative to the user's home directory.

### `FileName`

FileName is the credential file inside Dir.

### `const block`

Environment variables consulted by FromEnv.

### `Credentials`

Credentials identify an organization and the token used to act on its behalf.

### `Source`

Source records where a set of credentials came from, so the CLI can report it
without echoing the token itself.

### `ErrNotConfigured`

ErrNotConfigured is returned by Resolve when no source supplied credentials.

### `Credentials.Validate()`

Validate reports whether both fields are populated.

### `Credentials.complete()`

complete reports whether both fields are set, without allocating an error.

### `Credentials.Redacted()`

Redacted returns a copy safe to print or serialize in diagnostics.

### `Path()`

Path returns the absolute path of the credential file.

### `Load()`

Load reads credentials from disk. A missing file is reported as os.ErrNotExist,
so callers can distinguish "not configured yet" from a genuine read failure.

### `Save()`

Save writes credentials to disk with owner-only permissions.

### `FromEnv()`

FromEnv reads credentials from the environment. Unset variables yield empty
fields rather than an error, so the result can be layered under other sources.

### `Resolve()`

Resolve picks credentials from the first source that supplies both fields:
explicit flags, then the environment, then the config file.

Nothing is written to disk here. Credentials supplied through the environment
stay in the process; persisting them would leak the token onto the filesystem
of every CI runner and container that sets CENSYS_TOKEN.

### `homeDir()`

homeDir resolves the user's home directory, falling back to the conventional
environment variables for setups where os.UserHomeDir fails (some CI images).

---

## Package `render`

Package render writes results in the shapes a pipeline or a human needs.

Everything here streams: results are written as they arrive rather than
accumulated, so a large search does not have to fit in memory before the
first byte reaches stdout.

Directory: `internal/render`

Files: `records.go`, `render.go`

| Declaration | Description |
|---|---|
| `Observation()` | Observation renders one certificate observation range. Both the CLI and the |
| `Bucket()` | Bucket renders one aggregation bucket, carrying the aggregated field name so |
| `Format` | Format selects an output encoding. |
| `Formats` | Formats lists every supported format, for flag help and validation. |
| `ParseFormat()` | ParseFormat validates a format name. |
| `FormatNames()` | FormatNames renders the supported formats for help text. |
| `hostColumns` | hostColumns is the column order for host rows in Table and CSV output. |
| `Record` | Record is one result in both of the shapes the formats need: Doc for the JSON |
| `Stream` | Stream writes records incrementally in one format. Callers must call Close to |
| `NewStream()` | NewStream starts a stream in the given format. |
| `Stream.Host()` | Host writes one host record. |
| `Stream.Record()` | Record writes one result. Tabular formats use Columns and Values, emitting the |
| `Stream.Value()` | Value writes an arbitrary payload. In Table and CSV formats it falls back to |
| `Stream.Count()` | Count reports how many records have been written. |
| `Stream.Close()` | Close finishes the document and flushes any buffered writer. |
| `Stream.writeRow()` | writeRow emits one tabular row in whichever of CSV or Table is active. |
| `Stream.write()` | write appends to the active writer, remembering the first failure. |
| `hostRow()` | hostRow flattens a record into the columns declared by hostColumns. |

### `Observation()`

Observation renders one certificate observation range. Both the CLI and the
interactive menu emit these, so the column order lives here rather than at
either call site.

### `Bucket()`

Bucket renders one aggregation bucket, carrying the aggregated field name so
a saved file still says what it counted.

### `Format`

Format selects an output encoding.

### `Formats`

Formats lists every supported format, for flag help and validation.

### `ParseFormat()`

ParseFormat validates a format name.

### `FormatNames()`

FormatNames renders the supported formats for help text.

### `hostColumns`

hostColumns is the column order for host rows in Table and CSV output.

### `Record`

Record is one result in both of the shapes the formats need: Doc for the JSON
encodings, Columns and Values for the tabular ones.

### `Stream`

Stream writes records incrementally in one format. Callers must call Close to
finish the document; for JSON and Table that is where the trailing structure
is emitted.

### `NewStream()`

NewStream starts a stream in the given format.

### `Stream.Host()`

Host writes one host record.

### `Stream.Record()`

Record writes one result. Tabular formats use Columns and Values, emitting the
header on the first record; the JSON formats use Doc.

Once a stream has emitted a header, records with different columns fall back
to compact JSON in a single field, because a CSV cannot change shape midway.

### `Stream.Value()`

Value writes an arbitrary payload. In Table and CSV formats it falls back to
compact JSON in a single field, since such a value has no declared columns.

### `Stream.Count()`

Count reports how many records have been written.

### `Stream.Close()`

Close finishes the document and flushes any buffered writer.

### `Stream.writeRow()`

writeRow emits one tabular row in whichever of CSV or Table is active.

### `Stream.write()`

write appends to the active writer, remembering the first failure.

### `hostRow()`

hostRow flattens a record into the columns declared by hostColumns.

---

## Package `hunt`

Package hunt turns loosely typed operator input into the target lists the
Censys API expects.

Directory: `internal/hunt`

Files: `targets.go`

| Declaration | Description |
|---|---|
| `MaxCIDRHosts` | MaxCIDRHosts caps how many addresses one prefix may expand to. Every expanded |
| `ParseTargets()` | ParseTargets turns operator input into a deduplicated list of IP addresses. |
| `ExpandPrefix()` | ExpandPrefix lists every address in a prefix, refusing prefixes wider than |
| `ReadTargets()` | ReadTargets parses targets from a reader, one entry per line. |
| `ReadTargetsFile()` | ReadTargetsFile parses targets from a file, one entry per line. |

### `MaxCIDRHosts`

MaxCIDRHosts caps how many addresses one prefix may expand to. Every expanded
address becomes a metered lookup, so an accidental /8 must fail loudly rather
than silently queue 16 million billable requests.

### `ParseTargets()`

ParseTargets turns operator input into a deduplicated list of IP addresses.

Each entry may be a single IPv4/IPv6 address or a CIDR prefix, which is
expanded. Blank lines and lines starting with '#' are ignored. Unusable
entries are collected and returned alongside the targets that did parse, so
one typo in a large list does not discard the rest.

### `ExpandPrefix()`

ExpandPrefix lists every address in a prefix, refusing prefixes wider than
MaxCIDRHosts.

### `ReadTargets()`

ReadTargets parses targets from a reader, one entry per line.

### `ReadTargetsFile()`

ReadTargetsFile parses targets from a file, one entry per line.

---

## Package `ui`

Package ui implements the interactive menu.

Prompts, progress and status messages go to the message writer (stderr);
results go to the output writer (stdout). Keeping them apart is what lets
`censys > hits.ndjson` produce a clean file while the operator still sees
what is happening.

Directory: `internal/ui`

Files: `actions.go`, `ui.go`

| Declaration | Description |
|---|---|
| `usageWindowDays` | usageWindowDays is the reporting window for the credit usage summary. |
| `const block` | CenQL fields used to pivot from one host's artifacts to everything else |
| `exampleQueries` | exampleQueries are shown before the query prompt as a reminder of CenQL syntax. |
| `UI.offerPivots()` | offerPivots turns a result set into the next query. Extracting IPs, certificate |
| `UI.pivotQuery()` | pivotQuery lets the operator pick one fingerprint and runs the matching query. |
| `UI.bulkFetch()` | bulkFetch retrieves hosts through the batch endpoint, showing a progress bar |
| `UI.certHosts()` | certHosts pivots from a certificate to every host observed serving it. |
| `UI.timeline()` | timeline shows how a host's exposed services changed over a window. |
| `credentialPath()` | credentialPath renders the config path for prompts, degrading to the bare |
| `const block` | Status prefixes, kept for continuity with the previous console output. |
| `UI` | UI drives the interactive menu. |
| `Options` | Options configures a UI. |
| `New()` | New builds a UI, filling in the standard streams when they are not supplied. |
| `menuItem` | menuItem pairs a menu label with its handler. |
| `UI.Run()` | Run shows the menu until the operator exits or ctx is cancelled. |
| `UI.ask()` | ask prints a prompt to the message stream and reads one line from input. |
| `UI.askDefault()` | askDefault reads a line, substituting fallback when the operator just hits Enter. |
| `UI.askRequired()` | askRequired reads a line and rejects an empty answer. |
| `UI.confirm()` | confirm asks a yes/no question. def is the answer for a bare Enter. |
| `UI.stream()` | stream starts a render stream on the output writer in the selected format. |
| `nopCloser` | nopCloser adapts a Writer for promptui, which insists on a WriteCloser. |

### `usageWindowDays`

usageWindowDays is the reporting window for the credit usage summary.

### `const block`

CenQL fields used to pivot from one host's artifacts to everything else
presenting the same artifact. Platform queries require the dataset prefix:
a bare "services.port" matches nothing.

### `exampleQueries`

exampleQueries are shown before the query prompt as a reminder of CenQL syntax.

### `UI.offerPivots()`

offerPivots turns a result set into the next query. Extracting IPs, certificate
hashes and JARM fingerprints by hand is the tedious part of chasing
infrastructure, so the tool offers the follow-up directly.

### `UI.pivotQuery()`

pivotQuery lets the operator pick one fingerprint and runs the matching query.

### `UI.bulkFetch()`

bulkFetch retrieves hosts through the batch endpoint, showing a progress bar
on the message stream. The operator confirms first because every host costs a
credit whether or not Censys has a record for it.

### `UI.certHosts()`

certHosts pivots from a certificate to every host observed serving it.

### `UI.timeline()`

timeline shows how a host's exposed services changed over a window.

### `credentialPath()`

credentialPath renders the config path for prompts, degrading to the bare
filename if the home directory cannot be resolved.

### `const block`

Status prefixes, kept for continuity with the previous console output.

### `UI`

UI drives the interactive menu.

### `Options`

Options configures a UI.

### `New()`

New builds a UI, filling in the standard streams when they are not supplied.

### `menuItem`

menuItem pairs a menu label with its handler.

### `UI.Run()`

Run shows the menu until the operator exits or ctx is cancelled.

### `UI.ask()`

ask prints a prompt to the message stream and reads one line from input.

### `UI.askDefault()`

askDefault reads a line, substituting fallback when the operator just hits Enter.

### `UI.askRequired()`

askRequired reads a line and rejects an empty answer.

### `UI.confirm()`

confirm asks a yes/no question. def is the answer for a bare Enter.

### `UI.stream()`

stream starts a render stream on the output writer in the selected format.

### `nopCloser`

nopCloser adapts a Writer for promptui, which insists on a WriteCloser.

---

